# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Netlify
.netlify/

# Logs / deps (none expected for this static site)
node_modules/
*.log

# Source photo originals — optimised versions ship in assets/images/
shabbaimages/

# Local-only dev tooling: review screenshots, image-processing + screenshot
# scripts, and the npm deps they pull in (the site itself has no build step)
/_*.png
/_*.cjs
/process-images.cjs
/generate-logo.cjs
/package.json
/package-lock.json
